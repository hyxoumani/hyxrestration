docs/sessions/
results.tsv
run.log
evals/results/
*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/

# Secrets
.env

# Slice outputs (DuckDB + per-run reports)
data/
reports/

# Phase 0 data dumps — reproducible from yfinance, not committed
phase0/data/
# phase0/results/ IS committed — the falsification record is the deliverable

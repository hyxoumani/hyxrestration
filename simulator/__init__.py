"""simulator — replay + simulation side of the lab.

Sim engine, Strategy ABC + Context, capability contracts, book replay,
shadow harness, run entrypoints, and simui. May import the `hyxlab`
kernel only — never `collector` (enforced by tests/test_boundaries.py).
"""
